node_modules
dist
dist-ssr
*.local

# env: ship .env.example, never real values
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editors / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# local-only working notes: the mobile layout audit and its screenshots. Scoped rather than the
# whole directory, so a future tracked frontend/docs/ file is not silently swallowed.
/docs/mobile-audit.md
/docs/mobile-audit/
