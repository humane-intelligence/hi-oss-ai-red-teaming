# openapi-typescript@7 still declares a conservative peer `typescript@^5`, but the project runs
# TypeScript 6 (and the codegen works fine on it). Allow the install instead of failing ERESOLVE.
legacy-peer-deps=true
