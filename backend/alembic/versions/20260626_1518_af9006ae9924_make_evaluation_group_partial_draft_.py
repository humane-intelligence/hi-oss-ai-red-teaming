"""make evaluation group partial-draft columns nullable

Widening NOT NULL -> NULL on title/description/start_date so a partial draft
can be saved with only a title. `access_level` is deliberately NOT
widened: it keeps NOT NULL with a model-side default of `invitation_only` (a null
level would behave identically to `invitation_only` everywhere, so it buys nothing
but null-handling). Safe on existing rows. Downgrade re-imposes NOT NULL and will
fail if any draft rows hold a NULL in these columns — expected, drop/complete
those drafts first.

Revision ID: af9006ae9924
Revises: de0ad3da2fe0
Create Date: 2026-06-26 15:18:02.571306
"""

from collections.abc import Sequence

import sqlalchemy as sa

from alembic import op

revision: str = "af9006ae9924"
down_revision: str | None = "de0ad3da2fe0"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column("evaluation_groups", "title", existing_type=sa.VARCHAR(length=255), nullable=True)
    op.alter_column("evaluation_groups", "description", existing_type=sa.TEXT(), nullable=True)
    op.alter_column("evaluation_groups", "start_date", existing_type=sa.DATE(), nullable=True)
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.alter_column("evaluation_groups", "start_date", existing_type=sa.DATE(), nullable=False)
    op.alter_column("evaluation_groups", "description", existing_type=sa.TEXT(), nullable=False)
    op.alter_column("evaluation_groups", "title", existing_type=sa.VARCHAR(length=255), nullable=False)
    # ### end Alembic commands ###
